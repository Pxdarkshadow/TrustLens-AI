# TrustLens AI - API environment variables (all optional, sane defaults in code)
PORT=3001
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=15m
REFRESH_EXPIRES_DAYS=7
DATABASE_PATH=data/trustlens.db
STORAGE_DIR=storage